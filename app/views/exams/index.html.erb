<h1>Exams一覧</h1>
<% @exams.each do |exam| %>
  <%= exam.title %>
  <%= exam.deadline %>
  <%= exam.release %>
  <%= link_to 'Show', exam %>
  <%= link_to 'Edit', edit_exam_path(exam) %>
  <%= link_to 'Destroy', exam, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
